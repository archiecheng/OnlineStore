<% content_for :title, "New product" %>

<div class="max-w-3xl mx-auto space-y-4">
  <div>
    <p class="text-sm uppercase tracking-[0.2em] text-indigo-600 font-semibold">Admin</p>
    <h1 class="text-3xl font-bold text-gray-900">Create Product</h1>
    <p class="text-gray-600">Add a new book to the store catalog.</p>
  </div>

  <%= render "form", product: @product %>

  <div class="mt-2">
    <%= link_to "← Back", products_path, class: "text-indigo-600 hover:text-indigo-800 font-medium" %>
  </div>
</div>
