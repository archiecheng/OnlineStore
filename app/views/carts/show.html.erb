<% content_for :title, "Your Cart" %>

<h1 class="text-2xl font-bold mb-4">Your Cart</h1>

<% if @cart.cart_items.any? %>
  <%= render @cart %>
<% else %>
  <p>Your cart is empty.</p>
  <%= link_to "Browse products", products_path, class: "link link-primary" %>
<% end %>
