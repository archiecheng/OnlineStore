<% content_for :title, "Order ##{@order.id}" %>

<div class="space-y-4">
  <%= render @order %>
  <div class="flex gap-3">
    <%= link_to "Back to orders", orders_path, class: "px-4 py-2 rounded-lg border border-gray-200 hover:border-indigo-300 text-indigo-700 font-semibold" %>
    <%= link_to "Continue shopping", products_path, class: "px-4 py-2 rounded-lg bg-indigo-600 hover:bg-indigo-700 text-white font-semibold shadow-sm" %>
  </div>
</div>
